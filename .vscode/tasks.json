{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Hammerspoon tests",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "exit_code=$(hs -c \"local r = require('tests'); print('EXIT:' .. (r.failed > 0 and 1 or 0))\" | tail -1 | cut -d: -f2); exit ${exit_code:-1}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      }
    }
  ]
}
